let salaries = {
  John: 100,
  Ann: 160,
  Pete: 130,
};

console.log(salaries.John + salaries.Ann + salaries.Pete);

/**  if the property is a lot use for loop
 * let sum = 0;
 * for(let key in salaries){
 *  sum += salaries[key]
 * }
 */
